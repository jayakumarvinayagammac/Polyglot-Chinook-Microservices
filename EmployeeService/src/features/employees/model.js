/**
 * Employee model (JS doc style) - used for reference in services/repo
 * {
 *   id: string,
 *   firstName: string,
 *   lastName: string,
 *   title?: string,
 *   email?: string,
 *   phone?: string,
 *   createdAt: string,
 *   updatedAt: string
 * }
 */

// kept intentionally minimal - repository produces actual objects
module.exports = {};
